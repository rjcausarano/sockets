syntax = "proto3";

message Header {

  enum Type {
    COMMAND = 0;
    REQUEST = 1;
    RESPONSE = 2;
  }

  enum DataType {
    STRING = 0;
    ENTITY = 1;
  }

  Type type = 1;
  string data = 2;
}